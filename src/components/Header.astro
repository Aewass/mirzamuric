---
import { Image } from 'astro:assets';
import avatar from '../assets/image.svg';
import { Download } from 'lucide-astro';
---

<header class="h-screen flex items-center justify-center px-4">
  <div class="flex flex-col md:flex-row items-center gap-12 max-w-4xl mx-auto">
    <div class="w-40">
      <Image
        src={avatar}
        alt="Profile Avatar"
        loading="eager"
        class="w-full h-full object-contain dark:invert"
      />
    </div>

    <div class="max-w-lg text-center md:text-left">
      <div class="overflow-hidden">
        <h1 class="text-4xl font-bold mb-6 reveal-text">Hi, I'm Mirza</h1>
      </div>
      <div class="overflow-hidden">
        <p class="text-xl text-gray-600 dark:text-gray-300 leading-relaxed mb-8 reveal-text delay-200">
          I'm a <b>Frontend Developer</b> who likes solving problems. I build web applications and websites with a focus on clean code and great design.
        </p>
      </div>
      <div class="flex justify-center md:justify-start">
        <a 
          href="/mirzamuric_cv.pdf" 
          download
          class="button-dark group px-8 py-3 bg-gray-800 text-white rounded-lg text-lg border border-gray-700 relative overflow-hidden text-center flex items-center gap-2"
        >
          <span class="relative z-10">Download CV</span>
          <Download class="w-5 h-5 relative z-10" />
        </a>
      </div>
    </div>
  </div>
</header>

<style>
  .button-light {
    transition: all 0.2s ease;
  }

  .button-light:hover {
    background-color: #f3f4f6;
    transform: translateY(-1px);
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
  }

  .button-dark {
    transition: all 0.2s ease;
  }

  .button-dark:hover {
    transform: translateY(-1px);
    background-color: #1f2937;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
  }

  .reveal-text {
    animation: revealText 0.8s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    opacity: 0;
    transform: translateY(1rem);
  }

  .delay-200 {
    animation-delay: 200ms;
  }

  @keyframes revealText {
    from {
      opacity: 0;
      transform: translateY(1rem);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .button-light,
    .button-dark {
      transition: none !important;
      transform: none !important;
    }
    .reveal-text {
      animation: none;
      opacity: 1;
      transform: none;
    }
  }

  .button-light::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(to right, transparent, rgba(255, 255, 255, 0.4), transparent);
    transition: left 0.5s ease;
  }

  .button-light:hover::before {
    left: 100%;
  }

  .button-dark::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(to right, transparent, rgba(255, 255, 255, 0.05), transparent);
    transition: left 0.5s ease;
  }

  .button-dark:hover::before {
    left: 100%;
  }
</style>
