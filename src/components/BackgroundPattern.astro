<div class="fixed inset-0 -z-10">
  <div class="absolute inset-0 bg-pattern opacity-[0.03] dark:opacity-[0.08] dark:text-gray-500" />
</div>

<style>
  .bg-pattern {
    background-image: 
      linear-gradient(90deg, currentColor 0.5px, transparent 0.5px),
      linear-gradient(180deg, currentColor 0.5px, transparent 0.5px);
    background-size: 80px 80px;
    background-position: center;
  }

  @media (prefers-reduced-motion: reduce) {
    .animate-pattern {
      animation: none;
    }
  }
</style> 